# Any ONE of these enables server-side AI generation. With none set, the app
# runs in demo mode (curated offline dataset) and users can bring their own
# key in Settings.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
